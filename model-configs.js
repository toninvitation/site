/* =========================================================
   TONInvitation — CONFIGURAÇÕES DE COMPATIBILIDADE

   A configuração principal de cada convite passa a ficar dentro
   da própria pasta do convite, num ficheiro chamado config.json.

   Assim, para adicionar um novo convite, não é necessário alterar
   este ficheiro nem escrever código novo para cada modelo.

   Este ficheiro continua a existir para manter compatibilidade com
   configurações antigas que já estavam no projeto.
   ========================================================= */

/* Exporta configurações centrais opcionais para o servidor Node.js. */
module.exports = {
  /* O Toy Story e o Cars usam agora config.json dentro das próprias pastas. */
};
