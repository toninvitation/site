/* =========================================================
   TONInvitation — ENDEREÇO PÚBLICO DO BACKEND

   Este ficheiro é público e pode ficar no GitHub Pages.
   NUNCA coloques aqui tokens, passwords ou chaves Shopify.

   Depois de alojar o server.js num serviço cloud, coloca aqui
   o endereço HTTPS desse backend.

   Exemplo:
   window.TON_API_BASE_URL = "https://api.toninvitation.com";
   ========================================================= */

/* Define o endereço do backend online. Deixa vazio para testes locais. */
window.TON_API_BASE_URL = "";
